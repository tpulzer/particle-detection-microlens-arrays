training:
  batch_size: 32
  epochs: 1000
  learning_rate: 0.0002
  validation_split: 0.2

resume:
  enabled: false

paths:
  checkpoint_dir: "checkpoints"
  resume_checkpoint: "checkpoints/last_checkpoint.pt"
  log_file: "logs/train_log.csv"